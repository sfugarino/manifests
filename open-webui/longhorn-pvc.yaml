apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-webui-longhorn-pvc
  namespace: open-webui
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
