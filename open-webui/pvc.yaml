apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-open-webui-pvc
  namespace: open-webui
spec:
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
