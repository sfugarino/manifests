# values.yaml
nfs:
  server: 192.168.1.75
  path: /nfs/docling

storageClass:
  name: docling-nfs-client
  provisionerName: k8s-sigs.io/docling-nfs-subdir-external-provisioner 

nodeSelector:
  nfs-provisioner: "true"
