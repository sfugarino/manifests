# values.yaml
nfs:
  server: 192.168.1.83
  path: /mnt/ssd/jupyter

storageClass:
  name: jubhub-nfs-client
  provisionerName: k8s-sigs.io/juphub-nfs-subdir-external-provisioner 

nodeSelector:
  nfs-provisioner: "true"
